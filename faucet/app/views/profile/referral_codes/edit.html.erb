<div class="page-header">
  <h1>Editing <%= link_to "Referral code #{@referral.id}", profile_referral_code_path(@referral) %></h1>
</div>

<%= form_for [:profile, @referral], :html => { :class => "form-horizontal", :role => "form" } do |f| -%>
  <%= render "form", { :f => f } %>
<% end -%>

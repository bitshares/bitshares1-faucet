- if current_user.unconfirmed_email?
  Please confirm your email address and your account registration will be finished.
  Click #{link_to 'here', user_confirmation_path} if you want to resend confirmation email
- else
  #add-email.container
    %h1 Add Email
    = form_for(current_user, :as => 'user', :url => finish_signup_path(current_user), :html => { role: 'form'}) do |f|
      - if current_user.errors.any?
        #error_explanation
          - current_user.errors.full_messages.each do |msg|
            = msg
            %br/
      .form-group
        = f.label :email
        .controls
          = f.text_field :email, :autofocus => true, :value => '', class: 'form-control input-lg', placeholder: 'Example: email@me.com'
          %p.help-block Please confirm your email address. No spam.
      .actions
        = f.submit 'Continue', :class => 'btn btn-primary'
